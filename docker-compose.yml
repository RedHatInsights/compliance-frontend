version: '3'
services:
  frontend:
    build: .
    ports:
      - 8002:8002
    volumes:
      - .:/frontend
      - node_modules:/frontend/node_modules

networks:
  default:
    external:
      name: docker_default # should match insights-upload's network name

volumes:
  node_modules:
